name: Release

on:
  push:
    tags: ["v*"]

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build:
    name: build (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-22.04
            ext: ""
            archive: tar.gz
          - name: macos-aarch64
            os: macos-latest
            ext: ""
            archive: tar.gz
          - name: windows-x86_64
            os: windows-latest
            ext: ".exe"
            archive: zip
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release-${{ matrix.name }}

      - name: Install Linux system deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libasound2-dev libudev-dev libx11-dev libxcursor-dev \
            libxinerama-dev libxrandr-dev libxi-dev libgl1-mesa-dev

      - name: Build
        run: cargo build --release --locked

      - name: Resolve version
        id: ver
        shell: bash
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Package (tar.gz)
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          mkdir -p dist
          tar -czf "dist/usagi-${{ steps.ver.outputs.version }}-${{ matrix.name }}.tar.gz" \
            -C target/release "usagi${{ matrix.ext }}"

      - name: Package (zip)
        if: matrix.archive == 'zip'
        shell: bash
        run: |
          mkdir -p dist
          7z a -tzip "dist/usagi-${{ steps.ver.outputs.version }}-${{ matrix.name }}.zip" \
            "./target/release/usagi${{ matrix.ext }}"

      - uses: actions/upload-artifact@v4
        with:
          name: usagi-${{ matrix.name }}
          path: dist/*

  build-web:
    name: build (wasm)
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-emscripten

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release-wasm

      - uses: mymindstorm/setup-emsdk@v14
        with:
          version: latest
          actions-cache-folder: emsdk-cache

      - name: Build
        env:
          EMCC_CFLAGS: -fwasm-exceptions -sSUPPORT_LONGJMP=wasm -s USE_LIBPNG=1 -s USE_OGG=1 -s USE_VORBIS=1
        run: cargo build --release --locked --target wasm32-unknown-emscripten

      - name: Resolve version
        id: ver
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Package
        run: |
          mkdir -p dist
          tar -czf "dist/usagi-${{ steps.ver.outputs.version }}-wasm.tar.gz" \
            -C target/wasm32-unknown-emscripten/release usagi.js usagi.wasm

      - uses: actions/upload-artifact@v4
        with:
          name: usagi-wasm
          path: dist/*

  publish:
    name: publish
    needs: [build, build-web]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
          # Created as a draft so the release can be reviewed (notes, attached
          # archives) before going public.
          draft: true
          # Tags with a hyphen (e.g. v0.1.0-dev.1) follow semver prerelease
          # convention and are flagged as such on GitHub.
          prerelease: ${{ contains(github.ref_name, '-') }}
